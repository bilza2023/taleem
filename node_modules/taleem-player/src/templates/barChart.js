export const barChart = {
    group(data) {
      return {
        title: data.find(d => d.name === "title")?.content ?? "",
        bars: data
          .filter(d => d.name === "bar")
          .map(d => ({
            label: d.label,
            value: d.value
          }))
      };
    }
  };
  