// node_modules/taleem-player/src/templates/titleSlide.js
var titleSlide = {
  group(data) {
    var _a;
    return {
      title: ((_a = data.find((d) => d.name === "title")) == null ? void 0 : _a.content) ?? ""
    };
  }
};

// node_modules/taleem-player/src/templates/barChart.js
var barChart = {
  group(data) {
    var _a;
    return {
      title: ((_a = data.find((d) => d.name === "title")) == null ? void 0 : _a.content) ?? "",
      bars: data.filter((d) => d.name === "bar").map((d) => ({
        label: d.label,
        value: d.value
      }))
    };
  }
};

// node_modules/taleem-player/src/templates/bulletList.js
var bulletList = {
  group(data) {
    return {
      bullets: data.filter((d) => d.name === "bullet").map((d) => d.content)
    };
  }
};

// node_modules/taleem-player/src/templates/twoColumnText.js
var twoColumnText = {
  group(data) {
    var _a;
    return {
      title: ((_a = data.find((d) => d.name === "title")) == null ? void 0 : _a.content) ?? "",
      left: data.filter((d) => d.name === "left").map((d) => d.content),
      right: data.filter((d) => d.name === "right").map((d) => d.content)
    };
  }
};

// node_modules/taleem-player/src/templates/eq.js
var eq = {
  group(data) {
    const lines = [];
    let current = null;
    for (const item of data) {
      if (!item.type.startsWith("sp")) {
        current = {
          type: item.type,
          content: item.content,
          spItems: []
        };
        lines.push(current);
      } else if (current) {
        current.spItems.push({
          type: item.type,
          content: item.content
        });
      }
    }
    return { lines };
  }
};

// node_modules/taleem-player/src/templates/imageSlide.js
var imageSlide = {
  group(data) {
    const img = data.find((d) => d.name === "image");
    return {
      image: (img == null ? void 0 : img.content) ?? null
    };
  }
};

// node_modules/taleem-player/src/templates/index.js
var slideTemplates = {
  titleSlide,
  bulletList,
  twoColumnText,
  eq,
  barChart,
  imageSlide
};

// node_modules/taleem-player/src/Player.js
var Player = class {
  constructor(deck) {
    this.deck = deck;
    this.slides = deck.deck;
  }
  // ===== OLD (kept for later timeline mode)
  getSlideAt(time) {
    return this.slides.find(
      (s) => time >= s.start && time <= s.end
    );
  }
  getFrame(time) {
    const slide = this.getSlideAt(time);
    if (!slide)
      return null;
    return this._buildFrame(slide, time);
  }
  // ===== NEW (for manual navigation)
  getFrameByIndex(index) {
    const slide = this.slides[index];
    if (!slide)
      return null;
    return this._buildFrame(slide, slide.end);
  }
  // ===== shared internal logic
  _buildFrame(slide, time) {
    const template = slideTemplates[slide.type];
    if (!template) {
      throw new Error(`Unsupported slide type: ${slide.type}`);
    }
    const visibleData = slide.data.filter(
      (item) => item.showAt === void 0 || item.showAt <= time
    );
    return {
      type: slide.type,
      regions: template.group(visibleData),
      meta: {
        start: slide.start,
        end: slide.end
      }
    };
  }
};
export {
  Player
};
//# sourceMappingURL=taleem-player.js.map
