// node_modules/taleem-slides/src/slides/TitleSlide.js
var TitleSlide = {
  type: "titleSlide",
  fromJSON(raw) {
    var _a, _b;
    const title = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "title")) == null ? void 0 : _b.content;
    if (!title)
      throw new Error("titleSlide: title required");
    return Object.freeze({
      type: "titleSlide",
      title,
      render() {
        return `
          <section class="slide titleSlide">
            <h1>${title}</h1>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/TitleAndSubtitleSlide.js
var TitleAndSubtitleSlide = {
  type: "titleAndSubtitle",
  fromJSON(raw) {
    var _a, _b, _c, _d;
    const title = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "title")) == null ? void 0 : _b.content;
    const subtitle = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "subtitle")) == null ? void 0 : _d.content;
    if (!title || !subtitle) {
      throw new Error("titleAndSubtitle: requires title and subtitle");
    }
    return Object.freeze({
      type: "titleAndSubtitle",
      title,
      subtitle,
      render() {
        return `
          <section class="slide titleAndSubtitle">
            <h1>${title}</h1>
            <h2>${subtitle}</h2>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/TitleAndParaSlide.js
var TitleAndParaSlide = {
  type: "titleAndPara",
  fromJSON(raw) {
    var _a, _b, _c, _d;
    const title = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "title")) == null ? void 0 : _b.content;
    const para = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "para")) == null ? void 0 : _d.content;
    if (!title || !para) {
      throw new Error("titleAndPara: requires title and para");
    }
    return Object.freeze({
      type: "titleAndPara",
      title,
      para,
      render() {
        return `
          <section class="slide titleAndPara">
            <h1>${title}</h1>
            <p>${para}</p>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/BulletListSlide.js
var BulletListSlide = {
  type: "bulletList",
  fromJSON(raw) {
    var _a;
    const bullets = (_a = raw.data) == null ? void 0 : _a.filter((d) => d.name === "bullet").map((d) => ({ content: d.content }));
    if (!(bullets == null ? void 0 : bullets.length)) {
      throw new Error("bulletList: requires at least one bullet");
    }
    return Object.freeze({
      type: "bulletList",
      bullets,
      render({ visibleCount = bullets.length, activeIndex = null } = {}) {
        return `
          <section class="slide bulletList">
            <ul>
              ${bullets.map((b, i) => {
          if (i >= visibleCount)
            return "";
          const cls = i === activeIndex ? "is-active" : i < activeIndex ? "is-dim" : "";
          return `<li class="${cls}">${b.content}</li>`;
        }).join("")}
            </ul>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/TwoColumnTextSlide.js
var TwoColumnTextSlide = {
  type: "twoColumnText",
  fromJSON(raw) {
    var _a, _b;
    const left = (_a = raw.data) == null ? void 0 : _a.filter((d) => d.name === "left").map((d) => ({ content: d.content }));
    const right = (_b = raw.data) == null ? void 0 : _b.filter((d) => d.name === "right").map((d) => ({ content: d.content }));
    if (!(left == null ? void 0 : left.length) || !(right == null ? void 0 : right.length)) {
      throw new Error("twoColumnText: requires left and right columns");
    }
    return Object.freeze({
      type: "twoColumnText",
      left,
      right,
      render({
        leftVisibleCount = left.length,
        rightVisibleCount = right.length,
        leftActiveIndex = null,
        rightActiveIndex = null
      } = {}) {
        return `
          <section class="slide twoColumnText">
            <div class="col left">
              ${left.map((l, i) => {
          if (i >= leftVisibleCount)
            return "";
          const cls = i === leftActiveIndex ? "is-active" : i < leftActiveIndex ? "is-dim" : "";
          return `<div class="${cls}">${l.content}</div>`;
        }).join("")}
            </div>
            <div class="col right">
              ${right.map((r, i) => {
          if (i >= rightVisibleCount)
            return "";
          const cls = i === rightActiveIndex ? "is-active" : i < rightActiveIndex ? "is-dim" : "";
          return `<div class="${cls}">${r.content}</div>`;
        }).join("")}
            </div>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/ImageSlide.js
var ImageSlide = {
  type: "imageSlide",
  fromJSON(raw) {
    var _a, _b;
    const src = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "image")) == null ? void 0 : _b.content;
    if (!src)
      throw new Error("imageSlide: image required");
    return Object.freeze({
      type: "imageSlide",
      src,
      render() {
        return `
          <section class="slide imageSlide">
            <img src="${src}" alt="" />
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/FillImageSlide.js
var FillImageSlide = {
  type: "fillImage",
  fromJSON(raw) {
    var _a, _b;
    const image = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "image")) == null ? void 0 : _b.content;
    if (!image)
      throw new Error("fillImage: image required");
    return Object.freeze({
      type: "fillImage",
      image,
      render() {
        return `
          <section class="slide fillImage">
            <img src="${image}" alt="" />
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/ImageWithTitleSlide.js
var ImageWithTitleSlide = {
  type: "imageWithTitle",
  fromJSON(raw) {
    var _a, _b, _c, _d;
    const src = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "image")) == null ? void 0 : _b.content;
    const title = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "title")) == null ? void 0 : _d.content;
    if (!src || !title) {
      throw new Error("imageWithTitle: image and title required");
    }
    return Object.freeze({
      type: "imageWithTitle",
      src,
      title,
      render() {
        return `
          <section class="slide imageWithTitle">
            <img src="${src}" alt="" />
            <h1>${title}</h1>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/ImageWithCaptionSlide.js
var ImageWithCaptionSlide = {
  type: "imageWithCaption",
  fromJSON(raw) {
    var _a, _b, _c, _d;
    const src = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "image")) == null ? void 0 : _b.content;
    const caption = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "caption")) == null ? void 0 : _d.content;
    if (!src || !caption) {
      throw new Error("imageWithCaption: image and caption required");
    }
    return Object.freeze({
      type: "imageWithCaption",
      src,
      caption,
      render() {
        return `
          <figure class="slide imageWithCaption">
            <img src="${src}" alt="" />
            <figcaption>${caption}</figcaption>
          </figure>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/ImageLeftBulletsRightSlide.js
var ImageLeftBulletsRightSlide = {
  type: "imageLeftBulletsRight",
  fromJSON(raw) {
    var _a, _b, _c;
    const image = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "image")) == null ? void 0 : _b.content;
    const bullets = (_c = raw.data) == null ? void 0 : _c.filter((d) => d.name === "bullet").map((d) => ({ content: d.content }));
    if (!image || !(bullets == null ? void 0 : bullets.length)) {
      throw new Error("imageLeftBulletsRight: image and bullets required");
    }
    return Object.freeze({
      type: "imageLeftBulletsRight",
      image,
      bullets,
      render({ visibleCount = bullets.length, activeIndex = null } = {}) {
        return `
          <section class="slide imageLeftBulletsRight">
            <img src="${image}" alt="" />
            <ul>
              ${bullets.map((b, i) => {
          if (i >= visibleCount)
            return "";
          const cls = i === activeIndex ? "is-active" : i < activeIndex ? "is-dim" : "";
          return `<li class="${cls}">${b.content}</li>`;
        }).join("")}
            </ul>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/ImageRightBulletsLeftSlide.js
var ImageRightBulletsLeftSlide = {
  type: "imageRightBulletsLeft",
  fromJSON(raw) {
    var _a, _b, _c;
    const image = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "image")) == null ? void 0 : _b.content;
    const bullets = (_c = raw.data) == null ? void 0 : _c.filter((d) => d.name === "bullet").map((d) => ({ content: d.content }));
    if (!image || !(bullets == null ? void 0 : bullets.length)) {
      throw new Error("imageRightBulletsLeft: image and bullets required");
    }
    return Object.freeze({
      type: "imageRightBulletsLeft",
      image,
      bullets,
      render({ visibleCount = bullets.length, activeIndex = null } = {}) {
        return `
          <section class="slide imageRightBulletsLeft">
            <ul>
              ${bullets.map((b, i) => {
          if (i >= visibleCount)
            return "";
          const cls = i === activeIndex ? "is-active" : i < activeIndex ? "is-dim" : "";
          return `<li class="${cls}">${b.content}</li>`;
        }).join("")}
            </ul>
            <img src="${image}" alt="" />
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/TableSlide.js
var TableSlide = {
  type: "table",
  fromJSON(raw) {
    var _a;
    const rows = (_a = raw.data) == null ? void 0 : _a.filter((d) => d.name === "row").map((d) => ({ cells: d.content }));
    if (!(rows == null ? void 0 : rows.length)) {
      throw new Error("table: requires at least one row");
    }
    return Object.freeze({
      type: "table",
      rows,
      render({ visibleCount = rows.length, activeIndex = null } = {}) {
        return `
          <table class="slide table">
            ${rows.map((row, i) => {
          if (i >= visibleCount)
            return "";
          const cls = i === activeIndex ? "is-active" : i < activeIndex ? "is-dim" : "";
          return `
                <tr class="${cls}">
                  ${row.cells.map((c) => `<td>${c}</td>`).join("")}
                </tr>
              `;
        }).join("")}
          </table>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/StatisticSlide.js
var StatisticSlide = {
  type: "statistic",
  fromJSON(raw) {
    var _a, _b, _c, _d;
    const label = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "label")) == null ? void 0 : _b.content;
    const value = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "value")) == null ? void 0 : _d.content;
    if (!label || value === void 0) {
      throw new Error("statistic: requires label and value");
    }
    return Object.freeze({
      type: "statistic",
      label,
      value,
      render() {
        return `
          <section class="slide statistic">
            <div class="stat-value">${value}</div>
            <div class="stat-label">${label}</div>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/BigNumberSlide.js
var BigNumberSlide = {
  type: "bigNumber",
  fromJSON(raw) {
    var _a, _b, _c, _d;
    const value = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "number")) == null ? void 0 : _b.content;
    const label = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "label")) == null ? void 0 : _d.content;
    if (!value)
      throw new Error("bigNumber: number required");
    return Object.freeze({
      type: "bigNumber",
      value,
      label,
      render() {
        return `
          <section class="slide bigNumber">
            <div class="number">${value}</div>
            ${label ? `<div class="label">${label}</div>` : ""}
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/BarChartSlide.js
var BarChartSlide = {
  type: "barChart",
  fromJSON(raw) {
    var _a;
    const bars = (_a = raw.data) == null ? void 0 : _a.filter((d) => d.name === "bar").map((d) => ({
      label: d.content.label,
      value: d.content.value
    }));
    if (!(bars == null ? void 0 : bars.length)) {
      throw new Error("barChart: requires at least one bar");
    }
    return Object.freeze({
      type: "barChart",
      bars,
      render({ visibleCount = bars.length, activeIndex = null } = {}) {
        return `
          <section class="slide barChart">
            <ul class="bars">
              ${bars.map((b, i) => {
          if (i >= visibleCount)
            return "";
          const cls = i === activeIndex ? "is-active" : i < activeIndex ? "is-dim" : "";
          return `
                  <li class="bar ${cls}">
                    <span class="bar-label">${b.label}</span>
                    <span class="bar-value">${b.value}</span>
                  </li>
                `;
        }).join("")}
            </ul>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/DonutChartSlide.js
var DonutChartSlide = {
  type: "donutChart",
  fromJSON(raw) {
    var _a;
    const segments = (_a = raw.data) == null ? void 0 : _a.filter((d) => d.name === "segment").map((d) => ({
      label: d.content.label,
      value: d.content.value
    }));
    if (!(segments == null ? void 0 : segments.length)) {
      throw new Error("donutChart: requires at least one segment");
    }
    return Object.freeze({
      type: "donutChart",
      segments,
      render({ visibleCount = segments.length, activeIndex = null } = {}) {
        return `
          <section class="slide donutChart">
            <ul>
              ${segments.map((s, i) => {
          if (i >= visibleCount)
            return "";
          const cls = i === activeIndex ? "is-active" : i < activeIndex ? "is-dim" : "";
          return `<li class="${cls}">${s.label}: ${s.value}</li>`;
        }).join("")}
            </ul>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/QuoteSlide.js
var QuoteSlide = {
  type: "quoteSlide",
  fromJSON(raw) {
    var _a, _b, _c, _d;
    const text = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "quote")) == null ? void 0 : _b.content;
    const author = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "author")) == null ? void 0 : _d.content;
    if (!text)
      throw new Error("quoteSlide: quote required");
    return Object.freeze({
      type: "quoteSlide",
      text,
      author,
      render() {
        return `
          <blockquote class="slide quoteSlide">
            <p>${text}</p>
            ${author ? `<footer>${author}</footer>` : ""}
          </blockquote>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/QuoteWithImageSlide.js
var QuoteWithImageSlide = {
  type: "quoteWithImage",
  fromJSON(raw) {
    var _a, _b, _c, _d, _e, _f;
    const quote = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "quote")) == null ? void 0 : _b.content;
    const image = (_d = (_c = raw.data) == null ? void 0 : _c.find((d) => d.name === "image")) == null ? void 0 : _d.content;
    const author = (_f = (_e = raw.data) == null ? void 0 : _e.find((d) => d.name === "author")) == null ? void 0 : _f.content;
    if (!quote || !image) {
      throw new Error("quoteWithImage: quote and image required");
    }
    return Object.freeze({
      type: "quoteWithImage",
      quote,
      image,
      author,
      render() {
        return `
          <section class="slide quoteWithImage">
            <img src="${image}" alt="" />
            <blockquote>
              <p>${quote}</p>
              ${author ? `<footer>${author}</footer>` : ""}
            </blockquote>
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/CornerWordsSlide.js
var CornerWordsSlide = {
  type: "cornerWordsSlide",
  fromJSON(raw) {
    var _a;
    const words = (_a = raw.data) == null ? void 0 : _a.filter((d) => d.name === "word").map((d) => ({ content: d.content }));
    if (!(words == null ? void 0 : words.length)) {
      throw new Error("cornerWordsSlide: requires at least one word");
    }
    return Object.freeze({
      type: "cornerWordsSlide",
      words,
      render({ visibleCount = words.length } = {}) {
        return `
          <section class="slide cornerWordsSlide">
            ${words.map((w, i) => {
          if (i >= visibleCount)
            return "";
          return `<span class="corner-word corner-${i + 1}">${w.content}</span>`;
        }).join("")}
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/ContactSlide.js
var ContactSlide = {
  type: "contactSlide",
  fromJSON(raw) {
    var _a;
    const items = (_a = raw.data) == null ? void 0 : _a.map((d) => ({ content: d.content }));
    if (!(items == null ? void 0 : items.length)) {
      throw new Error("contactSlide: content required");
    }
    return Object.freeze({
      type: "contactSlide",
      items,
      render() {
        return `
          <section class="slide contactSlide">
            ${items.map((i) => `<div>${i.content}</div>`).join("")}
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/EqSlide.js
var EqSlide = {
  type: "eq",
  fromJSON(raw) {
    if (!Array.isArray(raw.data)) {
      throw new Error("eq: data must be array");
    }
    const lines = raw.data.map((d) => ({
      content: d.content
    }));
    return Object.freeze({
      type: "eq",
      lines,
      render({ activeIndex = null } = {}) {
        return `
          <section class="slide eq">
            ${lines.map((l, i) => {
          const cls = i === activeIndex ? "is-active" : i < activeIndex ? "is-dim" : "";
          return `<div class="eq-line ${cls}">${l.content}</div>`;
        }).join("")}
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/slides/SvgPointerSlide.js
var SvgPointerSlide = {
  type: "svgPointer",
  fromJSON(raw) {
    var _a, _b;
    const svg = (_b = (_a = raw.data) == null ? void 0 : _a.find((d) => d.name === "svg")) == null ? void 0 : _b.content;
    if (!svg)
      throw new Error("svgPointer: svg required");
    return Object.freeze({
      type: "svgPointer",
      svg,
      render() {
        return `
          <section class="slide svgPointer">
            ${svg}
          </section>
        `;
      }
    });
  }
};

// node_modules/taleem-slides/src/SlideTemplates.js
var SlideTemplates = {
  titleSlide: TitleSlide,
  titleAndSubtitle: TitleAndSubtitleSlide,
  titleAndPara: TitleAndParaSlide,
  bulletList: BulletListSlide,
  twoColumnText: TwoColumnTextSlide,
  imageSlide: ImageSlide,
  fillImage: FillImageSlide,
  imageWithTitle: ImageWithTitleSlide,
  imageWithCaption: ImageWithCaptionSlide,
  imageLeftBulletsRight: ImageLeftBulletsRightSlide,
  imageRightBulletsLeft: ImageRightBulletsLeftSlide,
  table: TableSlide,
  statistic: StatisticSlide,
  bigNumber: BigNumberSlide,
  barChart: BarChartSlide,
  donutChart: DonutChartSlide,
  quoteSlide: QuoteSlide,
  quoteWithImage: QuoteWithImageSlide,
  cornerWordsSlide: CornerWordsSlide,
  contactSlide: ContactSlide,
  eq: EqSlide,
  svgPointer: SvgPointerSlide
};

// node_modules/taleem-slides/src/getSlideTemplate.js
function getSlideTemplate(type) {
  const template = SlideTemplates[type];
  if (!template) {
    throw new Error(`Unknown slide template type "${type}"`);
  }
  return template;
}

// node_modules/taleem-slides/src/resolveBackground.js
function resolveBackground({ deckBackground, theme }) {
  if (deckBackground)
    return deckBackground;
  return {
    backgroundColor: theme.surfaceColor,
    backgroundImage: null,
    backgroundImageOpacity: 1
  };
}
export {
  SlideTemplates,
  getSlideTemplate,
  resolveBackground
};
//# sourceMappingURL=taleem-slides.js.map
